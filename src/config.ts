export interface Config {
  site: { name: string; description: string; themeColor: string; };
}

export const config: Config = {
  site: { name: 'Doctor Portal', description: '', themeColor: '#090a0b' },
};
